{
  "name": "whatsapp-team-inbox-docs",
  "version": "1.0.0",
  "description": "Mintlify documentation for WhatsApp Team Inbox",
  "scripts": {
    "dev": "mintlify dev",
    "build": "npm install -g mintlify && mintlify build",
    "start": "serve out -l ${PORT:-3000}",
    "preview": "mintlify preview",
    "railway:build": "npm install -g mintlify && mintlify build",
    "railway:start": "npx serve out -l 3000"
  },
  "dependencies": {
    "serve": "^14.2.1"
  },
  "devDependencies": {},
  "keywords": [
    "documentation",
    "mintlify",
    "whatsapp",
    "team-inbox",
    "self-hosted"
  ],
  "repository": {
    "type": "git",
    "url": "https://github.com/shadowpint/whatsapp-team-inbox-docs.git"
  },
  "author": "Reachbox Team",
  "license": "MIT",
  "engines": {
    "node": ">=18.0.0"
  }
}
